import { LogoIcon, ArrowIcon } from '../../../assets';
import { Header } from '../../../widgets/header';

export const MainLayout = () => {
  return (
    <div>
      <Header />
      Main Layout
    </div>
  );
};
